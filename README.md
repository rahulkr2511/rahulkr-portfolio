# Portfolio

Next.js 14 (App Router) + TypeScript + Tailwind. One content file (`lib/data.ts`)
drives the whole site — edit that, nothing else, for day-to-day updates.


## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.
